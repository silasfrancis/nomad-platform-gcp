[Unit]
Description=Prune unused Docker images, containers, networks, and build cache
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker system prune -af --filter until={{ docker_prune_until }}{{ ' --volumes' if docker_prune_include_volumes else '' }}
