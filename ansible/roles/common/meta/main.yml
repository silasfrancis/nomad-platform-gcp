---
galaxy_info:
  role_name: common
  description: >
    Base packages, OS hardening, and sysctl tuning shared across every
    node role. Docker is intentionally excluded — see roles/docker.
  min_ansible_version: "2.15"

dependencies: []
