{
  "_comment": "Reference only — Octopus itself is configured via Octopus.Server CLI flags in tasks/main.yml, not by reading this file. Kept here for human debugging/audit.",
  "instance": "{{ octopus_instance }}",
  "database_server": "localhost,1433",
  "database_name": "OctopusDeploy",
  "web_listen_prefix": "http://localhost:{{ octopus_web_port }}",
  "home_directory": "{{ octopus_home_dir }}"
}
