---
# Runs Against traefik-prod Only (Public-Subnet VM, boutique.lefrancis.org).

- name: Configure traefik-prod (Public)
  hosts: traefik_prod_public
  become: true
  vars:
    traefik_active_instances:
      - prod-public
  roles:
    - traefik
